#!/bin/sh

if [ "$(id -u)" -ne 0 ]; then
	echo "This must be run by root" >&2
	exit 1
fi

save_file="$DOTFILES/pacman/pkglist.txt"

pacman -S --needed $(comm -12 <(pacman -Slq | sort) <(sort $save_file))
