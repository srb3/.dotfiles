#!/bin/sh

work_apps="alacritty firefox slack zoom"

case $1 in
start)
	for app in $work_apps; do
		if [ -n "$(pgrep -f $app)" ]; then
			echo "$app already running"
			continue
		fi
		$app &
	done
	;;
end)
	for app in $work_apps; do
		killall -SIGTERM $app
	done
	;;
esac
