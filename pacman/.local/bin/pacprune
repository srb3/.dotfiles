#!/bin/sh

if [ "$(id -u)" -ne 0 ]; then
	err_message="This must be run by root"
	echo $err_message >&2
	notify-send -u normal " $err_message "
	exit 1
fi

save_file="$DOTFILES/pacman/pkglist.txt"

pacman -Rsu $(comm -23 <(pacman -Qq | sort) <(sort $save_file))
